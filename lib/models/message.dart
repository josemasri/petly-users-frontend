class Message {
  final int id, petId;
  final String email, message, type;
  final DateTime createdAt;

  Message({
    this.id,
    this.petId,
    this.email,
    this.message,
    this.type,
    this.createdAt,
  });
}
